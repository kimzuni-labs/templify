---
name: Release Please

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  release-please:
    uses: kimzuni-labs/workflows/.github/workflows/release-please.yml@main
    permissions:
      contents: write
      issues: write
      pull-requests: write
    with:
      generate-release-notes: true
